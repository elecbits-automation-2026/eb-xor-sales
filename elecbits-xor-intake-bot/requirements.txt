# XOR Intake Bot — runtime dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# LLM (real mode). Swap for litellm if you prefer the LiteLLM proxy pattern.
anthropic>=0.40

# Google Drive + Sheets backbone (real mode)
google-api-python-client>=2.140
google-auth>=2.30
google-auth-httplib2>=0.2
